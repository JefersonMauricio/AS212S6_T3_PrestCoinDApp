version: '2'

services:
  app:
    image: alexito03/pib:latest
    container_name: pibserver
    ports:
      - "4200:4200"
    #environment:
    #  MY_ENVIRONMENT: ${MY_ENVIRONMENT}
    #volumes:
    #  - /ruta/en/el/host:/ruta/en/el/contenedor